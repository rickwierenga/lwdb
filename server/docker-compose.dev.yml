version: "3.9"

services:
  db:
    ports:
      - "5433:5432"

  web:
    deploy:
      mode: replicated
      replicas: 1
    volumes:
      - "./:/app"
    environment:
      FLASK_ENV: "development"
    entrypoint: ["flask", "run", "--host=0.0.0.0", "--port=5001"]
